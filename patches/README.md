# Local patches

## fusionpbx-local.patch
Local modifications to upstream FusionPBX (`/var/www/fusionpbx`, branch tracking
`fusionpbx/fusionpbx`). Re-apply with:

    cd /var/www/fusionpbx && git apply /usr/ictcore/patches/fusionpbx-local.patch
